# binary packages
deb {{ common_apt_mirror }} {{ common_release_code }} main multiverse restricted universe
deb {{ common_apt_mirror }} {{ common_release_code }}-updates main multiverse restricted universe
deb {{ common_apt_mirror }} {{ common_release_code }}-proposed main multiverse restricted universe
deb {{ common_apt_mirror }} {{ common_release_code }}-backports main multiverse restricted universe
deb {{ common_apt_mirror }} {{ common_release_code }}-security  main multiverse restricted universe

# # sources
# deb-src {{ common_apt_mirror }} {{ common_release_code }} main multiverse restricted universe
# deb-src {{ common_apt_mirror }} {{ common_release_code }}-updates main multiverse restricted universe
# deb-src {{ common_apt_mirror }} {{ common_release_code }}-proposed main multiverse restricted universe
# deb-src {{ common_apt_mirror }} {{ common_release_code }}-backports main multiverse restricted universe
# deb-src {{ common_apt_mirror }} {{ common_release_code }}-security  main multiverse restricted universe
